services:
  - type: web
    name: instagram-unfollower-checker
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 server.py --host 0.0.0.0 --port $PORT --no-port-fallback
    healthCheckPath: /api/health
